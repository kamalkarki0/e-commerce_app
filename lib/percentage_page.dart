import 'package:flutter/material.dart';

class PercentagePage extends StatelessWidget {
  const PercentagePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        AppBar(
          title: const Text('Percentage Page'),
        )
      ],
    );
  }
}
